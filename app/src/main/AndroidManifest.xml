<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoastShinersSales"
        tools:targetApi="31">
        <activity
            android:name=".viewBindings.SinglePageActivity"
            android:exported="false" />
        <activity
            android:name=".viewBindings.ResetPassword"
            android:exported="false" />
        <activity
            android:name=".viewBindings.OtpPassActivity"
            android:exported="false" />
        <activity
            android:name=".viewBindings.ForgotPassword"
            android:exported="false" />
        <activity
            android:name=".viewBindings.OtpActivity"
            android:exported="false" />
        <activity
            android:name=".viewBindings.NavigationActivity"
            android:exported="false" />
        <activity
            android:name=".viewBindings.SignUpPage"
            android:exported="false" />
        <activity
            android:name=".viewBindings.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Add LoginPage Activity Declaration -->
        <activity android:name=".viewBindings.LoginPage" />
    </application>

</manifest>